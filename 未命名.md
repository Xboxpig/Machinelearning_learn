 记录一次训练线性回归模型并计算 mae 的函数
 第一次写的:
```
import numpy as np
def poly3():
    """
    参数:
    无

    返回:
    mae -- 预测结果的 MAE 评价指标
    """

    ### 代码开始 ### (≈ 7 行代码)
    model = LinearRegression()
    model.fit (X_train,y_train)
    for i in range (len(X_test)):
        y_pred = model.predict(X_test[i])
        y_real = y_test[i]
        mae_curr = 0
        mae_curr += np.avg(np.abs((y_real - y_pred)/y_real))
    
    mae = mae_curr
    ### 代码结束 ###

    return mae
```
Out[1]:
```
---------------------------------------------------------------------------
KeyError                                  Traceback (most recent call last)
/opt/conda/lib/python3.7/site-packages/pandas/core/indexes/base.py in get_loc(self, key, method, tolerance)
   2645             try:
-> 2646                 return self._engine.get_loc(key)
   2647             except KeyError:

pandas/_libs/index.pyx in pandas._libs.index.IndexEngine.get_loc()

pandas/_libs/index.pyx in pandas._libs.index.IndexEngine.get_loc()

pandas/_libs/hashtable_class_helper.pxi in pandas._libs.hashtable.PyObjectHashTable.get_item()

pandas/_libs/hashtable_class_helper.pxi in pandas._libs.hashtable.PyObjectHashTable.get_item()

KeyError: 0

During handling of the above exception, another exception occurred:

KeyError                                  Traceback (most recent call last)
<ipython-input-55-9bc531a6987e> in <module>
----> 1 poly3()

<ipython-input-54-f250b393c46b> in poly3()
     13     model.fit (X_train,y_train)
     14     for i in range (len(X_test)):
---> 15         y_pred = model.predict(X_test[i])
     16         y_real = y_test[i]
     17         mae_curr = 0

/opt/conda/lib/python3.7/site-packages/pandas/core/frame.py in __getitem__(self, key)
   2798             if self.columns.nlevels > 1:
   2799                 return self._getitem_multilevel(key)
-> 2800             indexer = self.columns.get_loc(key)
   2801             if is_integer(indexer):
   2802                 indexer = [indexer]

/opt/conda/lib/python3.7/site-packages/pandas/core/indexes/base.py in get_loc(self, key, method, tolerance)
   2646                 return self._engine.get_loc(key)
   2647             except KeyError:
-> 2648                 return self._engine.get_loc(self._maybe_cast_indexer(key))
   2649         indexer = self.get_indexer([key], method=method, tolerance=tolerance)
   2650         if indexer.ndim > 1 or indexer.size > 1:

pandas/_libs/index.pyx in pandas._libs.index.IndexEngine.get_loc()

pandas/_libs/index.pyx in pandas._libs.index.IndexEngine.get_loc()

pandas/_libs/hashtable_class_helper.pxi in pandas._libs.hashtable.PyObjectHashTable.get_item()

pandas/_libs/hashtable_class_helper.pxi in pandas._libs.hashtable.PyObjectHashTable.get_item()

KeyError: 0
```

更正:

```
import numpy as np
def poly3():
    """
    参数:
    无

    返回:
    mae -- 预测结果的 MAE 评价指标
    """

    ### 代码开始 ### (≈ 7 行代码)
    model = LinearRegression()
    model.fit (X_train,y_train)
    mae_curr = 0
    y_pred = model.predict(X_test)
# 3. 计算 MAE (取代你所有的循环和累加逻辑)
    # mean_absolute_error 函数会自动计算所有样本的平均绝对误差
    mae = mean_absolute_error(y_test, y_pred)
    ### 代码结束 ###

    return mae
```

```
def poly3():
   
    poly = PolynomialFeatures(degree=3)
    
    X_train_poly = poly.fit_transform(X_train)
    
    X_test_poly = poly.transform(X_test)
    

    model = LinearRegression()
    model.fit(X_train_poly, y_train)
    
    y_pred = model.predict(X_test_poly)

    mae = mean_absolute_error(y_test, y_pred)
    
    return mae
```

```
poly3()
28513637.376465987
```

```   
期望输出
1955.8027790596564
```